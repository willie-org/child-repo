name: E2E tests before release

on:
  workflow_dispatch:
  workflow_call:

jobs:
    e2e-tests:
      name: Run E2E tests
      runs-on: ubuntu-latest

      steps:
        - name: Checkout
          uses: actions/checkout@v5.0.0
        
        - name: Do work
          run: |
            echo "Success!"

        - name: Log context
          run: |
            ls -la
